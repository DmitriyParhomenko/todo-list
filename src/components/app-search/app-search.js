import React from 'react';

import './app-search.scss';

const AppSearch = () => {
    return (
        <input type="text"
               placeholder="Search"
               className="app-search"
        />
    );
};

export default AppSearch;